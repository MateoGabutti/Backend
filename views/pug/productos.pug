html
    head    
        title="ProductosPug"
        link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css", integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3", crossorigin="anonymous")
    body
        if objetos    
            table(class="table table-dark table-hover")   
                thead
                    tr
                        th(scope="col") Titulo
                        th(scope="col") Precio
                        th(scope="col") Imagen
                        th(scope="col") Id
                tbody
                    each item in objetos
                        tr
                            th(scope="row") #{item.title}
                            td $#{item.price}
                            td #{item.thumbnail}
                            td #{item.id}
        else 
            h3(class="text-center alert alert-primary") No hay productos
    a(href="http://localhost:3001" class="btn btn-dark col-3") Ir al formulario